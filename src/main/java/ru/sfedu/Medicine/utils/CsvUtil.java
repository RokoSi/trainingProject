package ru.sfedu.Medicine.utils;

import org.apache.logging.log4j.LogManager;

import java.util.logging.Logger;

public class CsvUtil {
    private static final Logger log = (Logger) LogManager.getLogger(CsvUtil.class.getName());
    private static final String PATH = Constants.CSV_PATH;
    private static final String EXTENSION = Constants.NAME_LIST_PATIENT;
    private static final String FILE_EXTENSION=".csv";


}
